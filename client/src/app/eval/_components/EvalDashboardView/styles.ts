import type { CSSProperties } from "react";

export const s = {
  page: { padding: "24px 32px 44px", maxWidth: 1100, margin: "0 auto" } satisfies CSSProperties,
  header: { display: "flex", alignItems: "center", gap: 14, marginBottom: 20 } satisfies CSSProperties,
  headerText: { flex: 1 } satisfies CSSProperties,
  h1: { fontSize: 24, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: {
    fontSize: 14,
    color: "var(--text-secondary)",
    marginTop: 4,
    maxWidth: 640,
  } satisfies CSSProperties,
  list: { display: "flex", flexDirection: "column", gap: 10 } satisfies CSSProperties,
  row: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 16,
    padding: "14px 18px",
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    textDecoration: "none",
    color: "var(--text-primary)",
  } satisfies CSSProperties,
  rowMain: { display: "flex", alignItems: "center", gap: 12, minWidth: 0, flex: 1 } satisfies CSSProperties,
  rowName: { fontSize: 15, fontWeight: 600, whiteSpace: "nowrap" } satisfies CSSProperties,
  modelChip: { border: "1px solid var(--border)", color: "var(--text-secondary)" } satisfies CSSProperties,
  lastRun: {
    fontSize: 12.5,
    color: "var(--text-muted)",
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
  } satisfies CSSProperties,
  rowMetrics: { display: "flex", alignItems: "center", gap: 20 } satisfies CSSProperties,
  metric: {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-end",
    fontSize: 15,
    fontWeight: 600,
    fontVariantNumeric: "tabular-nums",
    minWidth: 52,
  } satisfies CSSProperties,
  metricLabel: {
    fontSize: 10,
    fontWeight: 600,
    letterSpacing: "0.03em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  recent: { marginTop: 30 } satisfies CSSProperties,
  recentList: { marginTop: 10, display: "flex", flexDirection: "column", gap: 4 } satisfies CSSProperties,
  recentRow: {
    display: "grid",
    gridTemplateColumns:
      "minmax(140px,1.4fr) 128px 44px minmax(84px,1fr) minmax(84px,1fr) minmax(84px,1fr) 56px",
    alignItems: "center",
    gap: 14,
    padding: "9px 14px",
    borderRadius: 8,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    fontSize: 13,
  } satisfies CSSProperties,
  recentName: {
    fontWeight: 600,
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
  } satisfies CSSProperties,
  recentWhen: { color: "var(--text-secondary)", whiteSpace: "nowrap" } satisfies CSSProperties,
  recentVersion: {
    border: "1px solid var(--border)",
    color: "var(--text-secondary)",
    justifySelf: "start",
  } satisfies CSSProperties,
  recentPass: {
    textAlign: "right",
    fontWeight: 600,
    fontVariantNumeric: "tabular-nums",
  } satisfies CSSProperties,
  runError: {
    padding: "10px 14px",
    borderRadius: 8,
    border: "1px solid var(--warn)",
    background: "var(--warn-bg, transparent)",
    color: "var(--warn)",
    fontSize: 12.5,
    marginBottom: 16,
  } satisfies CSSProperties,
} as const;
