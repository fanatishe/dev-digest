/* PromptDiffPanel/constants.ts — colocated copy (see app/eval/constants.ts). */

export const COPY = {
  title: "System-prompt diff",
  unavailable: "Prompt unavailable — a compared version's config could not be resolved.",
  base: "base",
  head: "head",
} as const;
