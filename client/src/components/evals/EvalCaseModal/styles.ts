import type { CSSProperties } from "react";

/** Colocated styles for the EvalCaseModal (two-column body + footer). */
export const s = {
  body: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 20,
    padding: "20px 24px",
  } satisfies CSSProperties,
  col: { display: "flex", flexDirection: "column", gap: 14, minWidth: 0 } satisfies CSSProperties,
  expectationCard: (positive: boolean): CSSProperties => ({
    display: "flex",
    flexDirection: "column",
    gap: 8,
    padding: "12px 14px",
    borderRadius: 10,
    border: "1px solid " + (positive ? "var(--accent)" : "var(--warn)"),
    background: "var(--bg-surface)",
  }),
  expectationDesc: {
    fontSize: 13,
    color: "var(--text-secondary)",
    lineHeight: 1.5,
  } satisfies CSSProperties,
  fieldLabel: {
    fontSize: 12,
    fontWeight: 700,
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
    marginBottom: 6,
  } satisfies CSSProperties,
  required: { color: "var(--crit)", marginLeft: 3 } satisfies CSSProperties,
  filesLayout: { display: "grid", gridTemplateColumns: "140px 1fr", gap: 12 } satisfies CSSProperties,
  fileList: { display: "flex", flexDirection: "column", gap: 2 } satisfies CSSProperties,
  fileItem: (active: boolean): CSSProperties => ({
    textAlign: "left",
    padding: "6px 8px",
    borderRadius: 6,
    border: "none",
    cursor: "pointer",
    fontSize: 12.5,
    color: active ? "var(--text-primary)" : "var(--text-secondary)",
    background: active ? "var(--bg-hover)" : "transparent",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
  }),
  filePreview: {
    margin: 0,
    padding: 10,
    borderRadius: 8,
    background: "var(--bg-primary)",
    border: "1px solid var(--border)",
    fontSize: 12,
    lineHeight: "18px",
    maxHeight: 260,
    overflow: "auto",
    whiteSpace: "pre",
  } satisfies CSSProperties,
  empty: { fontSize: 13, color: "var(--text-muted)", padding: "8px 0" } satisfies CSSProperties,
  metaFields: { display: "flex", flexDirection: "column", gap: 12 } satisfies CSSProperties,
  editorHeader: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    marginBottom: 8,
  } satisfies CSSProperties,
  editorSpacer: { flex: 1 } satisfies CSSProperties,
  runError: {
    marginTop: 12,
    padding: "8px 12px",
    borderRadius: 8,
    border: "1px solid var(--warn)",
    background: "var(--bg-surface)",
    fontSize: 12.5,
    color: "var(--warn)",
  } satisfies CSSProperties,
  runResultWrap: { marginTop: 12 } satisfies CSSProperties,
  footer: {
    display: "flex",
    alignItems: "center",
    gap: 12,
  } satisfies CSSProperties,
  runToggle: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 13,
    color: "var(--text-secondary)",
  } satisfies CSSProperties,
  footerActions: {
    marginLeft: "auto",
    display: "flex",
    gap: 8,
  } satisfies CSSProperties,
} as const;
