# reviewer-core/docs — deep design reference

Stable design docs for the pure review engine: the diff → prompt → LLM → grounded
findings pipeline, the grounding gate, structured-output (Zod → JSON Schema),
map-reduce (`reduce`), and the optional prompt slots lessons feed it.

Single source of truth — `reviewer-core/CLAUDE.md` *links* here, never copies.

Suggested: `pipeline.md`, `grounding.md`, `prompt-slots.md`.
