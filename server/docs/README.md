# server/docs — deep design reference

Stable, human-authored design docs for `@devdigest/api`: architecture, request/DI
flow internals, DB schema notes, review-context assembly.

**This is a single source of truth.** `server/CLAUDE.md` *links* here with a
"Read when…" pointer — it never copies this content. Keep detail here; keep the
map in CLAUDE.md.

Suggested docs to add over time: `schema.md` (why every table is pre-declared),
`di-container.md`, `review-context.md` (repo-intel → prompt wiring).
