/** Constants for the project-context discovery module. */

/**
 * The only file type surfaced as an attachable Project-context document.
 * Compared case-insensitively against `extname(name).toLowerCase()`.
 */
export const CONTEXT_DOC_EXT = '.md';
